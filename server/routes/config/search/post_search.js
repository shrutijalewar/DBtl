'use strict';

//var Joi = require('joi');

module.exports = {
    description: 'Post Search',
    notes: 'Post a search',
    tags: ['search', 'post'],
    handler: function(request, reply){
        reply('OK');
    }
};

