'use strict';

//var Joi = require('joi');

module.exports = {
    description: 'Delete Search',
    notes: 'Delete a search',
    tags: ['search', 'delete'],
    handler: function(request, reply){
        reply('OK');
    }
};
