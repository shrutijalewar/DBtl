'use strict';

//var Joi = require('joi');

module.exports = {
    description: 'Slideshow',
    notes: 'Show image slideshow',
    tags: ['slideshow'],
    handler: function(request, reply){
        reply('OK');
    }
};
